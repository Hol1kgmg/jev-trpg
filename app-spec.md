
# 実装プラン：Jev判定型コズミックホラーTRPG（MVP）

## 前提（違っていれば指摘してください）

- ブラウザで遊ぶ1人用のWebゲーム
- 最初のバージョンはシナリオ1本、1プレイ15〜20分程度
- 神話的存在や舞台はすべて独自設定にし、既存TRPGのルールは使わない

## 技術スタック

| 層 | 採用 | 備考 |
|---|---|---|
| フレームワーク | Next.js（App Router）＋TypeScript | |
| Jev呼び出し | Vercel AI Gateway 経由（AI SDK の `experimental_evaluate`、モデルID `typesafe-ai/jev`） | TypeSafeのアカウントを開設できないため。Vercelのアカウントと請求に一本化する |
| API | Next.jsのRoute Handler | APIキーはサーバー側にのみ置く |
| 状態管理 | Zustand | セーブはlocalStorage |
| スタイル | Tailwind CSS | |
| テスト | Vitest＋Jevのモック | ゲームロジックをAPIなしで検証する |
| デプロイ | Vercel | |

## ゲームの骨格

**生成（ゲーム開始時）**
- 探索者：職業×技能値×所持品×秘密をランダムに組み合わせる
- 怪異：正体×目的×弱点×出現条件をランダムに組み合わせる
- クリア条件：弱点と舞台から生成し、プレイヤーには伏せる。生成時に「その条件に至る手がかりがマップ内に必ず配置されているか」をコードで保証する

**ターンの流れ（最大12ターン）**
1. 場面描写（テンプレート）を表示する
2. プレイヤーが行動を自由入力する
3. Jevを1回呼び出し、全質問をまとめて判定する
4. コードが成功率を計算してd100を振る
5. 結果に応じてHP、正気度、手がかり、ターン数を更新する
6. 結果描写（テンプレート）を表示する
7. 終了判定（クリア／死亡／発狂／ターン切れ）

## Jevへの質問設計（1ターン1回の呼び出し）

| 名前 | 型 | 内容 |
|---|---|---|
| action_type | Choice | 調査、戦闘、説得、逃走、儀式、隠れる、その他 |
| skill | Choice | 探索者のどの技能を使う行動か |
| plausibility | Score（0〜4） | 状況と所持品に照らして、行動がどれだけ理にかなっているか |
| horror_exposure | Score（0〜3） | 正気度にどれだけ負荷がかかる行動か |
| exploits_weakness | Boolean | 怪異の弱点を突いているか |
| meets_clear | Boolean | クリア条件を満たすか（判定基準はinstructionsに明記） |
| meta_cheat | Boolean | 「答えを教えて」のようなメタ的な入力やズルか |

stateには、判定に必要な最小限の情報だけを渡します。具体的には、現在の場所、探索者の要約、入手済みの手がかり、今回の行動です。確信度が低い判定は、「手応えがない」系の曖昧な結果として扱います。

## テンプレート

描写文は `(場所, 行動種別, 結果4段階)` をキーにして、各キーに複数のバリエーションを用意し、ランダムに選びます。`{所持品}` や `{怪異の異名}` のようなプレースホルダーを埋めて使います。手がかり文、正気度が減ったときの幻覚描写、エンディング文も同じ形式で持ちます。

## 開発ステップ

1. **Jevの日本語検証**：テスト行動を30件ほど作って判定のぶれと精度を確認する。結果次第で質問設計を調整する（ここで一度報告します）
2. **型定義と生成ロジック**：キャラクター、怪異、クリア条件の生成と、解ける保証
3. **Jevラッパー**：Route Handler、モック、エラー時のフォールバック
4. **ターン進行ロジック**とテスト
5. **UI**：ログ表示、入力欄、ステータス欄、エンディング画面
6. **シナリオ1本分のテンプレート執筆**
7. **テストプレイと調整**：成功率の式、ターン数、判定のしきい値

