# Vercel AI Gateway の API キー（ローカル開発用）。
# Vercel 本番では VERCEL_OIDC_TOKEN が自動注入されるため設定不要。
AI_GATEWAY_API_KEY=

# 封緘鍵。32 バイトを base64 で。生成: openssl rand -base64 32
SEAL_KEY=

# 1 のとき Jev をスタブに差し替え、実 API を呼ばない（API キーなしで遊べる）
JEV_STUB=1
